<UserControl x:Class="TwoViews.Views.FirstView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
	<Grid>
		

		<!--<Button Command="{Binding ShowPopUp}" Content="Show Pop Up" />
		<Button Grid.Row="1"
                Command="{Binding IncrementValue}"
                Content="Increment value" />
		<TextBlock
                   Grid.Column="1"
                   MinWidth="50"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Text="{Binding ExampleValue}"
                   TextAlignment="Center" Height="16" Width="50" Margin="2,142" />-->
		<TextBox HorizontalAlignment="Left" Height="27" Margin="100,16,0,0" TextWrapping="Wrap" Text="Login" VerticalAlignment="Top" Width="101"/>
		<TextBox HorizontalAlignment="Left" Height="27" Margin="100,58,0,0" TextWrapping="Wrap" Text="Mot de passe" VerticalAlignment="Top" Width="101"/>
		<Button
                    Command="{Binding SecondViewCommand}"
                    Content="Login" Margin="100,107,100,163" />
	</Grid>
</UserControl>
